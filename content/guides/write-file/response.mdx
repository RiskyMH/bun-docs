---
title: Write Response
fullTitle: Write a Response to a file
mode: center
---

Use [`Bun.write()`](/docs/runtime/file-io#writing-files-bun-write) to write a `Response` to disk. Bun consumes the `Response` body according to its `Content-Type` header.

The first argument is a _destination_, like an absolute path or `BunFile` instance. The second argument is the _data_ to write.

```ts
const result = await fetch("https://bun.com");
const path = "./file.txt";
await Bun.write(path, result);
```

---

See [`Bun.write()`](/docs/runtime/file-io#writing-files-bun-write).
